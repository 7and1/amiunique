# ==============================================
# AmiUnique.io Environment Variables
# Copy this file to .env.local for development
# ==============================================

# API Configuration
# -----------------
# URL of the Cloudflare Worker API
# Development: http://localhost:8787
# Production: https://api.amiunique.io
NEXT_PUBLIC_API_URL=http://localhost:8787

# Optional: Override API URL for SSR (server-side)
# API_URL=http://localhost:8787

# Cloudflare D1 Database
# ----------------------
# Database ID (used in wrangler.toml, not as env var)
# Create with: wrangler d1 create amiunique-db
# D1_DATABASE_ID=your-database-id

# Worker Environment
# ------------------
# Set in wrangler.toml [vars] section
# ENVIRONMENT=development | production

# Build Configuration
# -------------------
# Node environment
NODE_ENV=development

# Optional: Skip type checking during build (not recommended)
# SKIP_TYPE_CHECK=false

# Analytics (Optional)
# --------------------
# Cloudflare Web Analytics beacon token
# NEXT_PUBLIC_CF_BEACON_TOKEN=

# Development Helpers
# -------------------
# Enable debug logging
# DEBUG=amiunique:*

# Local D1 database file location (auto-managed by wrangler)
# .wrangler/state/v3/d1/amiunique-db/

# ==============================================
# AI Chat Assistant (Optional)
# ==============================================

# OpenRouter API Key
# ------------------
# Get your free API key at: https://openrouter.ai/keys
# The chat feature works WITHOUT an API key (uses intelligent fallback responses)
# Adding an API key enables OpenRouter integration for enhanced AI responses
# IMPORTANT: API keys are NEVER exposed to the client (server-side only)
OPENROUTER_API_KEY=

# Model Selection (Optional)
# ---------------------------
# Default: meta-llama/llama-3.3-70b-instruct:free (70B parameters, high quality)
# Other free options:
#   - google/gemini-2.0-flash-exp:free (fastest with 1M context)
#   - qwen/qwen3-coder:free (code-focused with 262K context)
#   - tngtech/deepseek-r1t-chimera:free (deep reasoning with CoT)
#   - nousresearch/hermes-3-llama-3.1-405b:free (405B parameters, slower but best quality)
# See all free models: https://openrouter.ai/models?q=free
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free

# Security Notes:
# - NEVER commit this file with real API keys
# - Use .env.local for local development (gitignored by default)
# - Use platform environment variables in production (Vercel, Cloudflare Pages, etc.)
# - API keys are only used server-side in the Next.js API route

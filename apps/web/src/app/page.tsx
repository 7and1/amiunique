import { HomeContent } from './home-content';

export const metadata = {
  title: 'Am I Unique? Free Browser Fingerprint Test | 80+ Dimensions',
  description: 'Test how unique your browser fingerprint is with our free tool. Analyze 80+ dimensions including canvas, WebGL, audio fingerprinting. See your tracking risk in seconds.',
};

export default function HomePage() {
  return <HomeContent />;
}
